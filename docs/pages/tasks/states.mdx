# 任务状态

顺序任务、并发任务和动作任务的执行状态如下图所示。每个任务被创建后处于 `PENDING` 状态表示等待执行。
当任务因为前驱节点失败或通过 `when` 指定的关系不满足，导致任务无法继续执行时，它会被设置为 `SKIPPED` 态。
当任务执行完毕后，若执行成功则会被设置为 `SUCCESS` 态，否则设置为 `FAILED` 态。

![States](/states.png)
